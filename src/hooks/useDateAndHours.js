import { use, useEffect, useState } from "react";

export const useDateAndHours = () => {

  const [dates, setDates] = useState([]);
  const [day, setDay] = useState({});
  const [hours, setHours] = useState([]);
  const [hour, setHour] = useState({});

  

  return {
    
  };
};